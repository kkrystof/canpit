import styled from "styled-components"


const Window = ({children, layout}) => {

    return <WindowTemp>

    </WindowTemp>

}

const WindowTemp = styled.div`
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 0;
      height: 100vh;
      width: 100vw;
      overflow: hidden;
      background: brown;
`
